import type { Metadata } from 'next'
export const metadata: Metadata = {
    title: 'Team | Maxsa App',
    description: 'Team description',
}
const Team = () => {
    return (
        <h1>
            Team
        </h1>
    );
};

export default Team;