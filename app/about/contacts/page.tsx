import type { Metadata } from 'next'
export const metadata: Metadata = {
    title: 'Contacts | Maxsa',
    description: 'Contacts description',
}
const Contacts = () => {
    return (
        <h1>
            Contacts
        </h1>
    );
};

export default Contacts;